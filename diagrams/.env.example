# ── Next.js Frontend (.env) ──

# Required: PostgreSQL connection string for diagram caching
POSTGRES_URL="postgresql://gitdiagram_user:localdev@localhost:5432/gitdiagram"

# Diagram backend URL (FastAPI service that generates diagrams)
NEXT_PUBLIC_API_DEV_URL=http://localhost:8000

# GitUnderstand ingestion API URL (for the repo digest/analysis features)
# GITUNDERSTAND_API_URL=http://localhost:8080

# Skip env validation during build (useful for Docker/CI)
# SKIP_ENV_VALIDATION=1
